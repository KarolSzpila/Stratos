/*
 * Airplaine.cpp
 *
 *  Created on: 29.10.2017
 *      Author: Karol
 */

#include <AircraftRecord.h>

AircraftRecord::AircraftRecord(const std::string& ICAO_Address) : ICAO_Address(ICAO_Address)
{
	leftLiveSpan = DEFAULT_LIVE_SAPAN;
	aircraftExpiered = false;
}

void AircraftRecord::UpdateStatus(const ADS_BMessage& msg)
{
	if(leftLiveSpan > 0)
	{
		--leftLiveSpan;
		if(leftLiveSpan == 0)
		{
			aircraftExpiered = true;
		}
	}
}
