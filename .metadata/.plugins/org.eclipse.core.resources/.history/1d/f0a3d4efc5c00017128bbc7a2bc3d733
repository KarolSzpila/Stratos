/*
 * FlightControlModel.cpp
 *
 *  Created on: 28.10.2017
 *      Author: Karol
 */

#include "FlightControl.h"


FlightControl::FlightControl()
{
}


AircraftRecord* FlightControl::FindAircraftByICAO_Address(const std::string& ICAO_Address)
{
	//auto it = aircrafts.find(ICAO_Address);
	//if(it == aircrafts.end()) { return NULL;}

	//return &it->second;
}

void FlightControl::AddAircraft(const std::string& ICAO, const AircraftRecord& aircraft)
{
	//aircrafts.insert(ICAO,aircraft);
}

void FlightControl::TickAircrafts()
{
	for(auto it = aircrafts.begin(); it != aircrafts.end(); it++)
	{

	}
}
